{
  "name": "gta",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel index.html",
    "build": "rm -rf dist && parcel build index.html",
    "deploy": "cp CNAME dist && surge dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.8.0",
    "phaser": "^3.55.2"
  }
}
